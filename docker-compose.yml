version: '3'
services:
  db: 
    image: mongo:latest
  backend:
    image: node:latest
    volumes:
      - .:/backend
    ports: 
      - 3000:3000
    command: bash -c "cd /backend && npm i && node app"